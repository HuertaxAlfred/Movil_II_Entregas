<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Se permite el acceso a internet aqui -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permisos para acceder al almacenamiento (Como tal a la base de datos) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <!-- Resto de la app -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DivisasRoom"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DivisasRoom">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Registro de mi content provider -->


        <provider
            android:name=".ENTIDADES.ContentProvider"
            android:authorities="com.example.divisasroom.provider"
            android:exported="true"
            android:grantUriPermissions="true" />

        <!--
        <provider
            android:name="com.example.divisasroom.ENTIDADES.ContentProvider"
            android:authorities="com.example.divisasroom.provider"
            android:exported="true"
            android:grantUriPermissions="true" />
            -->


        <!-- <provider
            android:name=".ExchangeRatesProvider"
            android:authorities="com.example.divisasroom.provider"
            android:readPermission="com.example.divisasroom.PERMISSION_READ" - - - - - - - - - - QUITAR ESTO: PERMISOS DE SOLO LECTURA
            android:writePermission="com.example.divisasroom.PERMISSION_WRITE"
            /> -->

    </application>

</manifest>